[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install --no-cache-dir -r requirements_railway.txt"]

[start]
cmd = "gunicorn app.app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 1"
