[build]
builder = "NIXPACKS"
buildCommand = "pip install -r requirements_railway.txt"

[deploy]
startCommand = "gunicorn app.app:app --bind 0.0.0.0:$PORT --timeout 300 --workers 1"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
